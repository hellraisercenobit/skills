---
"@hellraisercenobit/ai-engineering-gate": minor
---

Ship the engineering suite gate: a registry, per-task evidence, three fingerprints, and the commands that enforce them. The plugin installs the hooks; the npm package is a byte-identical distribution root.
