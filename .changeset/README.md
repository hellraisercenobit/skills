# Changesets

This folder is used by `@changesets/cli` to version this skills package.

- Run `npm run changeset` after a meaningful skill change
- Merging the version PR bumps `package.json` / changelog and tags the release
