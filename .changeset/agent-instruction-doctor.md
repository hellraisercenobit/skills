---
"@hellraisercenobit/ai-engineering-gate": minor
---

Add `agent-instruction-doctor`: diagnose ignored, conflicting or unreachable agent instructions, skills, hooks and settings, then propose the smallest patch. Remove `nuke-review`, `transpose-comments` and `review-comments` from the plugin; uninstall them if you installed them with `npx skills`.
