{
  "name": "testing-patterns-qualification",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude '**/*.browser.test.ts'",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "browser": "vitest run --config vitest.browser.config.ts"
  },
  "devDependencies": {
    "@types/node": "24.12.0",
    "@vitest/browser-playwright": "5.0.1",
    "fast-check": "4.10.1",
    "playwright": "1.63.0",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  }
}
